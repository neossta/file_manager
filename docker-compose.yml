version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - file_storage:/app/files
    environment:
      - NODE_ENV=development
    restart: unless-stopped

volumes:
  file_storage: